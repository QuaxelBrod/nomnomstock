Web Frontend (Next.js)
======================

This workspace will hold the Next.js app (PWA-ready) including `app/`, `components/`, `public/` and styles.

Planned steps:
- Move `app/` and `components/` here
- Adjust imports and build scripts
